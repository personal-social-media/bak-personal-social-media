<div class="px-2 mx-auto md:w-1/2 w-full">
  <div class="flex">
    <div>
      <%= render(Person::AvatarComponent.new(profile: current_peer, class: "w-64 h-64 object-cover rounded-full")) %>
    </div>

    <div class="md:ml-20 flex-1">
      <%= render partial: "profiles/show/info", locals: { current_peer: current_peer } %>
    </div>
  </div>

  <div>
    Stories
  </div>

  <div class="mx-auto bg-gray-100 w-1/3">
    Feed
    <%= react_component("profile-feed",
                        peer: current_peer.as_json(only: %i(name username ip avatars)).deep_transform_keys! { |key| key.camelize(:lower) }
        ) %>
  </div>
</div>
